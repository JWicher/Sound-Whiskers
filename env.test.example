# Playwright E2E Test Environment Variables
# Copy this file to .env.test and fill in your test credentials
# DO NOT commit .env.test to version control (it's already in .gitignore)

# ==============================================================================
# PLAYWRIGHT CONFIGURATION
# ==============================================================================
PLAYWRIGHT_BASE_URL=http://localhost:3000

# ==============================================================================
# SUPABASE TEST INSTANCE (CLOUD)
# ==============================================================================
# Use a SEPARATE test instance, not your production database!
# Create a new project on Supabase specifically for E2E testing
NEXT_PUBLIC_SUPABASE_URL=https://your-test-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_test_supabase_anon_key_here

# ==============================================================================
# E2E TEST USER CREDENTIALS
# ==============================================================================
# Create a dedicated test user in your Supabase test instance:
# 1. Sign up the user through your app or Supabase dashboard
# 2. Verify the email address (disable email verification in Supabase for test instance if needed)
# 3. Copy the user's UUID from Supabase dashboard > Authentication > Users
# 4. Fill in the credentials below

E2E_USERNAME_ID=00000000-0000-0000-0000-000000000000
E2E_USERNAME=test-user@example.com
E2E_PASSWORD=YourSecureTestPassword123!

# ==============================================================================
# SETUP INSTRUCTIONS
# ==============================================================================
# 1. Copy this file:
#    cp env.test.example .env.test
#
# 2. Create a test Supabase project:
#    - Go to https://supabase.com/dashboard
#    - Create a new project for testing
#    - Run migrations: npx supabase db push (if using local migrations)
#
# 3. Create a test user:
#    - Sign up through your app at http://localhost:3000/auth/register
#    - OR create directly in Supabase dashboard
#    - Verify the email (or disable email verification in test instance)
#
# 4. Get user credentials:
#    - Email and password you used during signup
#    - UUID from Supabase dashboard > Authentication > Users
#
# 5. Fill in values above with your test credentials
#
# 6. Run tests:
#    npm run test:e2e

